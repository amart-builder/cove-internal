/** Small, maintained rules from observed failures. Keep incident history and
 * proof in PLANNING_RELIABILITY.md, not in every model invocation. */
export const PLANNING_LESSONS = `Lessons from verified planning failures:
1. Reuse the work's identity. Check existingTasks before proposing preparation. If a task already covers the outcome, select that task as source with proposal=null, or set proposal.existingTask to its ref.N when a calendar event supplies the timing. Mentioning "use the existing task" in prose does not link records. Do not create separate work for a narrower next step of the same task. Use existingTask=null only for distinct new work. Do not guess a match from a person's name alone; different outcomes remain separate.
2. Preserve what a time means. A task follow-up date, a booked calendar event, and a proposed internal review are different facts. When asking about an existing follow-up, name the person and the action (for example, "check whether Sam Lee has booked") and use the saved task date. Never replace that date with nextCheckAt. Generated time references receive a proposed-time label from Cove. Prefer omitting internal clocks from user questions unless the clock changes the decision. Missing notification evidence means unverified, not a promise that no reminder will occur.
3. Carry corrections forward. Reuse recorded answers and current completion state. A rescheduled meeting can change urgency without creating another preparation task. Completed work is not missing preparation. Preserve the person's started day and never treat a clarification as completion without a separate explicit completion action.`;
